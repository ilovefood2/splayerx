const store = {
  commit() {},
  dispatch: () => Promise.resolve(),
  getters: {},
  hasModule: () => false,
  registerModule() {},
  state: {},
  unregisterModule() {},
  watch: () => () => {},
};

export default store;
