# Standing Rules

- After every code or configuration change, bump the application version in
  `package.json`. Use a patch bump unless the user requests a different release
  level, and include the bump in the same commit as the change.
- Always build and verify a fresh application after making changes.
